Options -Indexes

<FilesMatch "^\.env$">
  Require all denied
</FilesMatch>

<FilesMatch "^(chatbot-config|chatbot-lib)\.php$">
  Require all denied
</FilesMatch>

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^data/chatbot_logs/ - [F,L]
</IfModule>

